class Solution {
public:
    bool canAliceWin(vector<int>& nums) {
        int singles = 0, doubles = 0;

        for (int n : nums) {
            if (n < 10) singles += n;
            else        doubles += n;
        }

        return singles != doubles;
    }
};
