class Solution {
public:
    int addDigits(int num) {
        
        int one = 0, sum=0;

        while(num>9){
            one = num%10;
            num = num/10;
            num+=one;
            
        }
        
        return num;
    }
};
